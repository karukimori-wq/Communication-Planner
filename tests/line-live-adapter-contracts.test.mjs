import assert from "node:assert/strict";import{readFileSync}from"node:fs";import{describe,it}from"node:test";const read=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8");describe("LINE live adapter contracts",()=>{it("keeps dry-run as the safe default",()=>{const s=read("src/lib/adapters/line.ts");assert.match(s,/request\.deliveryMode!=="live"/);assert.match(s,/line-dry-run/);});it("uses LINE push API only after the live gate",()=>{const s=read("src/lib/adapters/line.ts");assert.match(s,/https:\/\/api\.line\.me\/v2\/bot\/message\/push/);assert.match(s,/LINE_CHANNEL_ACCESS_TOKEN/);assert.match(s,/authorization:`Bearer \$\{token\}`/);assert.match(s,/"x-line-retry-key":request\.idempotencyKey/);assert.match(s,/to:request\.externalUserId/);assert.match(s,/text:request\.body/);});it("never returns the credential value",()=>{const s=read("src/lib/adapters/line.ts");assert.doesNotMatch(s,/token[^\n]*reason|externalMessageId:token/);});});
