import assert from"node:assert/strict";import{readFileSync}from"node:fs";import{describe,it}from"node:test";const read=p=>readFileSync(new URL(`../${p}`,import.meta.url),"utf8");describe("native LINE webhook contracts",()=>{it("accepts LINE native event identifiers and source user",()=>{const w=read("src/lib/adapters/webhook.ts");assert.match(w,/webhookEventId/);assert.match(w,/source/);assert.match(w,/userId/);assert.match(w,/groupId/);assert.match(w,/roomId/);});it("routes workspace outside the signed LINE body",()=>{const r=read("src/lib/adapters/webhook-route.ts");assert.match(r,/x-communication-workspace-id/);assert.match(r,/searchParams\.get\("workspaceId"\)/);assert.match(r,/workspaceId:routedWorkspace/);});it("persists provider webhook messages to D1 in production",()=>{const r=read("src/lib/adapters/webhook-route.ts");assert.match(r,/getPersistenceReadiness/);assert.match(r,/readiness\.driver==="d1"/);assert.match(r,/D1CommunicationRepository/);assert.match(r,/ingestChannelMessage/);assert.match(r,/persistenceDriver:readiness\.driver/);});});
