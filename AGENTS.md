# Communication Planner Agent Instructions

Before substantial coding, debugging, adapter, safety, identity, deployment, integration, or production-readiness work, consult `karukimori-wq/ai-development-intelligence`.

Follow `core/autonomous-development-policy.md`: continue implementation → verification → investigation/fix → retest → related checks → documentation → next executable task. Do not stop after one small step. Ask only at a defined stop condition, and continue independent authorized work when possible.

Use the task retrieval protocol, ranking/index, and `memory/communication-planner/`. Pay particular attention to webhook identity, wrong-recipient prevention, hard send gates, external side effects, and provider readiness.

For substantial tasks, follow `core/task-run-standard.md`, `core/operational-learning-loop.md`, and the strict failure-outcome attribution policy.

Current Communication Planner implementation is authoritative for code. `professional-platform-contracts` is authoritative for formal contracts. Development intelligence is advisory and never overrides either.

After meaningful work, evaluate extraction and capture only reusable discoveries, failures/root causes, decisions, patterns, contradictions, or stable evidence.

Never store secrets, provider credentials, tokens, message/customer personal data, or raw sensitive payloads in development intelligence.