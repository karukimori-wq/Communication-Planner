import { getCloudflareContext } from "@opennextjs/cloudflare";
import { fail,ok,readJson,requestMeta,requireString } from "@/lib/http";
import { getPersistenceReadiness } from "@/lib/persistence/driver";
import { D1CommunicationRepository,type D1DatabaseLike } from "@/lib/persistence/d1";
import { createReplyDraft } from "@/lib/store";
import type { CreateReplyDraftInput } from "@/lib/types";
type RouteContext={params:Promise<{conversationId:string}>};
export async function POST(request:Request,context:RouteContext){const meta=requestMeta(request),{conversationId}=await context.params,body=await readJson<Partial<CreateReplyDraftInput>>(request);if(!body)return fail("INVALID_JSON","Request body must be JSON",400,meta);const workspaceId=requireString(body.workspaceId,"workspaceId");if(typeof workspaceId!=="string")return fail(workspaceId.code,workspaceId.message,400,meta);const personId=requireString(body.personId,"personId");if(typeof personId!=="string")return fail(personId.code,personId.message,400,meta);const purpose=requireString(body.purpose,"purpose");if(typeof purpose!=="string")return fail(purpose.code,purpose.message,400,meta);const readiness=getPersistenceReadiness();if(readiness.driver==="d1")try{const db=getCloudflareContext().env.DB as unknown as D1DatabaseLike;const draft=await new D1CommunicationRepository(db).createReplyDraft({workspaceId,personId,conversationId,purpose,body:body.body});if(!draft)return fail("CONVERSATION_SCOPE_MISMATCH","Conversation must exist and belong to the provided workspaceId and personId",404,meta);return ok({replyDraft:draft,persistenceDriver:"d1"},{...meta,eventName:"communication.reply_draft.created.v1"});}catch{return fail("PERSISTENCE_ERROR","Reply draft could not be persisted",503,meta);}const draft=await createReplyDraft({workspaceId,personId,conversationId,purpose,body:body.body});if(!draft)return fail("CONVERSATION_SCOPE_MISMATCH","Conversation must exist and belong to the provided workspaceId and personId",404,meta);return ok({replyDraft:draft,persistenceDriver:readiness.driver},{...meta,eventName:"communication.reply_draft.created.v1"});}
