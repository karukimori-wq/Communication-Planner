export type D1RunResult={success:boolean;meta?:unknown};export type D1PreparedStatement={bind(...values:unknown[]):D1PreparedStatement;first<T=Record<string,unknown>>():Promise<T|null>;run():Promise<D1RunResult>;all<T=Record<string,unknown>>():Promise<{results:T[];success:boolean}>};export type D1DatabaseLike={prepare(query:string):D1PreparedStatement;batch(statements:D1PreparedStatement[]):Promise<D1RunResult[]>};type Scope={workspaceId:string;personId:string;conversationId:string};type PersistedChannel="line"|"x"|"instagram";export type D1ChannelMessageInput={workspaceId:string;channel:PersistedChannel;externalUserId:string;body:string;direction:"inbound"|"outbound";externalMessageId?:string;externalThreadId?:string;displayName?:string;personId?:string;conversationId?:string;topics?:string[];promises?:string[];nextActions?:string[]};
export class D1CommunicationRepository{constructor(private readonly db:D1DatabaseLike){}async healthcheck(){const row=await this.db.prepare("SELECT 1 AS ok").first<{ok:number}>();return row?.ok===1;}async getPersonScope(workspaceId:string,personId:string){return this.db.prepare("SELECT id,workspace_id,display_name,profile_summary,created_at,updated_at FROM communication_persons WHERE workspace_id=? AND id=? LIMIT 1").bind(workspaceId,personId).first();}async listInbox(workspaceId:string){return(await this.db.prepare("SELECT c.id AS conversationId,c.person_id AS personId,p.display_name AS displayName,c.channel,m.body AS lastMessagePreview,c.last_message_at AS lastMessageAt FROM conversations c JOIN communication_persons p ON p.workspace_id=c.workspace_id AND p.id=c.person_id LEFT JOIN messages m ON m.id=(SELECT m2.id FROM messages m2 WHERE m2.workspace_id=c.workspace_id AND m2.conversation_id=c.id ORDER BY m2.created_at DESC LIMIT 1) WHERE c.workspace_id=? ORDER BY c.last_message_at DESC").bind(workspaceId).all()).results;}async listPersonConversations(workspaceId:string,personId:string){return(await this.db.prepare("SELECT id AS conversationId,workspace_id AS workspaceId,person_id AS personId,channel,external_thread_id AS externalThreadId,status,last_message_at AS lastMessageAt,created_at AS createdAt,updated_at AS updatedAt FROM conversations WHERE workspace_id=? AND person_id=? ORDER BY last_message_at DESC").bind(workspaceId,personId).all()).results;}async getConversationScope(workspaceId:string,personId:string,conversationId:string){return this.db.prepare("SELECT id,workspace_id,person_id,channel_identity_id,channel,external_thread_id,status,last_message_at,created_at,updated_at FROM conversations WHERE workspace_id=? AND person_id=? AND id=? LIMIT 1").bind(workspaceId,personId,conversationId).first<{id:string;channel:PersistedChannel}>();}async getPersonContext(workspaceId:string,personId:string){return this.db.prepare("SELECT id,workspace_id,person_id,summary,promises_json,next_actions_json,updated_at FROM conversation_contexts WHERE workspace_id=? AND person_id=? LIMIT 1").bind(workspaceId,personId).first();}
async createReplyDraft(input:Scope&{purpose:string;body?:string}){const conversation=await this.getConversationScope(input.workspaceId,input.personId,input.conversationId);if(!conversation)return null;const now=new Date().toISOString(),id=crypto.randomUUID(),body=input.body??"";await this.db.prepare("INSERT INTO reply_drafts (id,workspace_id,person_id,conversation_id,body,status,created_at,updated_at) VALUES (?,?,?,?,?,'draft',?,?)").bind(id,input.workspaceId,input.personId,input.conversationId,body,now,now).run();return{replyDraftId:id,workspaceId:input.workspaceId,personId:input.personId,conversationId:input.conversationId,body,purpose:input.purpose,status:"draft",createdAt:now,updatedAt:now};}
async ingestChannelMessage(input:D1ChannelMessageInput){const now=new Date().toISOString(),externalThreadId=input.externalThreadId??`${input.channel}:${input.externalUserId}`;if(input.externalMessageId){const d=await this.db.prepare("SELECT id,person_id,conversation_id FROM messages WHERE workspace_id=? AND channel=? AND external_message_id=? LIMIT 1").bind(input.workspaceId,input.channel,input.externalMessageId).first<{id:string;person_id:string;conversation_id:string}>();if(d)return{duplicate:true,messageId:d.id,personId:d.person_id,conversationId:d.conversation_id};}const ei=await this.db.prepare("SELECT id,person_id FROM channel_identities WHERE workspace_id=? AND channel=? AND channel_user_id=? LIMIT 1").bind(input.workspaceId,input.channel,input.externalUserId).first<{id:string;person_id:string}>(),personId=input.personId??ei?.person_id??crypto.randomUUID(),channelIdentityId=ei?.id??crypto.randomUUID(),conversationId=input.conversationId??crypto.randomUUID(),messageId=crypto.randomUUID(),s:D1PreparedStatement[]=[];const ep=await this.getPersonScope(input.workspaceId,personId);if(!ep)s.push(this.db.prepare("INSERT INTO communication_persons(id,workspace_id,display_name,profile_summary,created_at,updated_at)VALUES(?,?,?,NULL,?,?)").bind(personId,input.workspaceId,input.displayName??input.externalUserId,now,now));if(!ei)s.push(this.db.prepare("INSERT INTO channel_identities(id,workspace_id,person_id,channel,channel_user_id,display_name,created_at,updated_at)VALUES(?,?,?,?,?,?,?,?)").bind(channelIdentityId,input.workspaceId,personId,input.channel,input.externalUserId,input.displayName??null,now,now));let c=input.conversationId?await this.getConversationScope(input.workspaceId,personId,input.conversationId):null;if(!c)c=await this.db.prepare("SELECT id,channel FROM conversations WHERE workspace_id=? AND person_id=? AND channel=? AND external_thread_id=? LIMIT 1").bind(input.workspaceId,personId,input.channel,externalThreadId).first<{id:string;channel:PersistedChannel}>();const cid=c?.id??conversationId;if(!c)s.push(this.db.prepare("INSERT INTO conversations(id,workspace_id,person_id,channel_identity_id,channel,external_thread_id,status,last_message_at,created_at,updated_at)VALUES(?,?,?,?,?,?,'open',?,?,?)").bind(cid,input.workspaceId,personId,channelIdentityId,input.channel,externalThreadId,now,now,now));else s.push(this.db.prepare("UPDATE conversations SET last_message_at=?,updated_at=? WHERE workspace_id=? AND person_id=? AND id=?").bind(now,now,input.workspaceId,personId,cid));s.push(this.db.prepare("INSERT INTO messages(id,workspace_id,person_id,conversation_id,channel,external_message_id,direction,body,received_at,created_at)VALUES(?,?,?,?,?,?,?,?,?,?)").bind(messageId,input.workspaceId,personId,cid,input.channel,input.externalMessageId??messageId,input.direction,input.body,now,now));s.push(this.db.prepare("INSERT INTO conversation_contexts(id,workspace_id,person_id,summary,promises_json,next_actions_json,updated_at)VALUES(?,?,?,?,?,?,?) ON CONFLICT(workspace_id,person_id) DO UPDATE SET summary=excluded.summary,promises_json=excluded.promises_json,next_actions_json=excluded.next_actions_json,updated_at=excluded.updated_at").bind(crypto.randomUUID(),input.workspaceId,personId,input.body,JSON.stringify(input.promises??[]),JSON.stringify(input.nextActions??[]),now));for(const label of input.topics??[])s.push(this.db.prepare("INSERT INTO topics(id,workspace_id,person_id,conversation_id,source_message_id,label,created_at)VALUES(?,?,?,?,?,?,?)").bind(crypto.randomUUID(),input.workspaceId,personId,cid,messageId,label,now));for(const body of input.promises??[])s.push(this.db.prepare("INSERT INTO promises(id,workspace_id,person_id,conversation_id,source_message_id,body,created_at)VALUES(?,?,?,?,?,?,?)").bind(crypto.randomUUID(),input.workspaceId,personId,cid,messageId,body,now));for(const body of input.nextActions??[])s.push(this.db.prepare("INSERT INTO communication_next_actions(id,workspace_id,person_id,conversation_id,source_message_id,body,status,created_at)VALUES(?,?,?,?,?,?,'open',?)").bind(crypto.randomUUID(),input.workspaceId,personId,cid,messageId,body,now));await this.db.batch(s);return{duplicate:false,messageId,personId,conversationId:cid};}
async getReplyDraft(scope:Scope&{replyDraftId:string}){return this.db.prepare("SELECT id,workspace_id,person_id,conversation_id,body,status,created_at,updated_at FROM reply_drafts WHERE id=? AND workspace_id=? AND person_id=? AND conversation_id=? LIMIT 1").bind(scope.replyDraftId,scope.workspaceId,scope.personId,scope.conversationId).first();}async updateReplyDraft(scope:Scope&{replyDraftId:string;body:string;updatedAt:string}){const d=await this.getReplyDraft(scope);if(!d)throw new Error("REPLY_DRAFT_SCOPE_MISMATCH");return this.db.prepare("UPDATE reply_drafts SET body=?,status='draft',updated_at=? WHERE id=? AND workspace_id=? AND person_id=? AND conversation_id=?").bind(scope.body,scope.updatedAt,scope.replyDraftId,scope.workspaceId,scope.personId,scope.conversationId).run();}async getLatestSafetyCheck(scope:Scope&{replyDraftId:string}){return this.db.prepare("SELECT id,status FROM safety_checks WHERE reply_draft_id=? AND workspace_id=? AND person_id=? AND conversation_id=? ORDER BY checked_at DESC LIMIT 1").bind(scope.replyDraftId,scope.workspaceId,scope.personId,scope.conversationId).first<{id:string;status:string}>();}async createSafetyCheck(input:Scope&{safetyCheckId:string;replyDraftId:string;status:"passed"|"blocked"|"warning";issues:string[];checkedAt:string}){const d=await this.getReplyDraft(input);if(!d)throw new Error("SAFETY_CHECK_SCOPE_MISMATCH");return this.db.batch([this.db.prepare("INSERT INTO safety_checks(id,workspace_id,person_id,conversation_id,reply_draft_id,status,issues_json,checked_at)VALUES(?,?,?,?,?,?,?,?)").bind(input.safetyCheckId,input.workspaceId,input.personId,input.conversationId,input.replyDraftId,input.status,JSON.stringify(input.issues),input.checkedAt),this.db.prepare("UPDATE reply_drafts SET status=?,updated_at=? WHERE id=? AND workspace_id=? AND person_id=? AND conversation_id=?").bind(input.status==="passed"?"safety_checked":"blocked",input.checkedAt,input.replyDraftId,input.workspaceId,input.personId,input.conversationId)]);}async assertSendReady(scope:Scope&{replyDraftId:string}){const d=await this.getReplyDraft(scope);if(!d)throw new Error("REPLY_DRAFT_SCOPE_MISMATCH");const s=await this.getLatestSafetyCheck(scope);if(!s)throw new Error("SAFETY_CHECK_REQUIRED");if(s.status!=="passed")throw new Error("SAFETY_CHECK_FAILED");return{draft:d,safety:s};}async getSendDecisionByIdempotencyKey(workspaceId:string,key:string){return this.db.prepare("SELECT * FROM reply_send_decisions WHERE workspace_id=? AND idempotency_key=? LIMIT 1").bind(workspaceId,key).first();}async listSendDecisions(scope:Scope&{replyDraftId:string}){return(await this.db.prepare("SELECT * FROM reply_send_decisions WHERE workspace_id=? AND person_id=? AND conversation_id=? AND reply_draft_id=? ORDER BY decided_at DESC").bind(scope.workspaceId,scope.personId,scope.conversationId,scope.replyDraftId).all()).results;}async recordSuccessfulSend(input:Scope&{replyDraftId:string;sendDecisionId:string;messageId:string;externalMessageId:string;channel:PersistedChannel;body:string;contentHash:string;deliveryMode:"dry_run"|"live";adapterReference:string;idempotencyKey:string;adapterDelivery:unknown;decidedAt:string}){const e=await this.getSendDecisionByIdempotencyKey(input.workspaceId,input.idempotencyKey);if(e)return{duplicate:true,decision:e};const{safety}=await this.assertSendReady(input),c=await this.getConversationScope(input.workspaceId,input.personId,input.conversationId);if(!c)throw new Error("CONVERSATION_SCOPE_MISMATCH");if(c.channel!==input.channel)throw new Error("SEND_CHANNEL_MISMATCH");await this.db.batch([this.db.prepare("INSERT INTO messages(id,workspace_id,person_id,conversation_id,channel,external_message_id,direction,body,received_at,created_at)VALUES(?,?,?,?,?,?,'outbound',?,?,?)").bind(input.messageId,input.workspaceId,input.personId,input.conversationId,input.channel,input.externalMessageId,input.body,input.decidedAt,input.decidedAt),this.db.prepare("INSERT INTO reply_send_decisions(id,workspace_id,person_id,conversation_id,reply_draft_id,safety_check_id,message_id,channel,content_hash,delivery_mode,adapter_reference,idempotency_key,adapter_delivery_json,decided_at)VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(input.sendDecisionId,input.workspaceId,input.personId,input.conversationId,input.replyDraftId,safety.id,input.messageId,input.channel,input.contentHash,input.deliveryMode,input.adapterReference,input.idempotencyKey,JSON.stringify(input.adapterDelivery??{}),input.decidedAt),this.db.prepare("UPDATE reply_drafts SET status='sent',updated_at=? WHERE id=? AND workspace_id=? AND person_id=? AND conversation_id=?").bind(input.decidedAt,input.replyDraftId,input.workspaceId,input.personId,input.conversationId)]);return{duplicate:false,decision:await this.getSendDecisionByIdempotencyKey(input.workspaceId,input.idempotencyKey)};}}
